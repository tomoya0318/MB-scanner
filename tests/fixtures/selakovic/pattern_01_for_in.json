{
  "pattern_id": 1,
  "description": "for..in vs for loop over an array (result accumulates in setup-bound array)",
  "expected_verdict": "equal",
  "expected_primary_oracles": ["argument_mutation"],
  "setup": "const arr = [10, 20, 30]; const result = [];",
  "before": "for (const i in arr) { result.push(arr[i]); } result.length",
  "after": "for (let i = 0; i < arr.length; i++) { result.push(arr[i]); } result.length"
}
